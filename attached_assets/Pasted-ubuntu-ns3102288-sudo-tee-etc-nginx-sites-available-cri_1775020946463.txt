ubuntu@ns3102288:~$ sudo tee /etc/nginx/sites-available/critsend > /dev/null << 'NGINX'
upstream critsend_app {
    server 127.0.0.1:5000;
    keepalive 32;
}

map $http_upgrade $connection_upgrade {
    default upgrade;
    ""      close;
}

server {
    listen 80;
    listen [::]:80;
    server_name server.critsender.com critsender.rndamailing.com critsender.mayasoldes.com critsender.verysent.com critsender.mahlesoldes.com critsender.mayesale.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name server.critsender.com critsender.rndamailing.com critsender.mayasoldes.com critsender.verysent.com critsender.mahlesoldes.com critsender.mayesale.com;

    ssl_certificate /etc/letsencrypt/live/server.critsender.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/server.critsender.com/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 1d;

    add_header Strict-Transport-Security "max-age=63072000" always;

    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/javascript application/javascript application/json application/xml image/svg+xml font/woff font/woff2;

    client_max_body_size 1100m;
    client_body_timeout 600s;

    location ~ ^/api/jobs/stream {
        proxy_pass http://critsend_app;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 3600s;
        add_header X-Accel-Buffering no;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
sudo nginx -t && sudo systemctl reload nginxscheme;_x_forwarded_for;
nginx: [warn] duplicate value "TLSv1.2" in /etc/nginx/sites-enabled/critsend:35
nginx: [warn] duplicate value "TLSv1.3" in /etc/nginx/sites-enabled/critsend:35
nginx: [emerg] "ssl_prefer_server_ciphers" directive is duplicate in /etc/nginx/sites-enabled/critsend:36
nginx: configuration file /etc/nginx/nginx.conf test failed